<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="author" content="Авсюкевич Анастасия">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="keywords" content={{keywordsContent}}>
  <meta name="description" content={{descriptionContent}}>
  <title>{{titleContent}}</title>
  <link href="../public/styles/general.css" rel="stylesheet" />
  <link href={{customStyle}} rel="stylesheet" />
  {{> sharedLinks}}
</head>
<body>
<div class="wrapper">
  <script defer src="../public/src/general.js"></script>

  {{#if customScripts}}
    {{#each customScripts}}
      <script defer src="../public/src/{{this}}.js"></script>
    {{/each}}
  {{/if}}

  {{> stickClass}}


  {{> (lookup . 'partials/content')}}
  <!-- content  -->


  {{> line}}
  {{> footer}}
</div>
</body>
</html>
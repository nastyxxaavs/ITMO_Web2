<main>
  <div class="entities">
    {{#if alertMessage}}
      <script>
        alert("{{alertMessage}}");
      </script>
    {{/if}}
      {{#if contacts.length}}
         <h1>Контакты компании</h1>
          <ul>
            {{#each contacts}}
              <li>
                <div class="entity-content">
                <a href="/contacts/{{this.id}}"></a> - {{this.address}}, {{this.phone}}, {{this.email}}, {{this.mapsLink}}
                <a href="/contact-edit/{{this.id}}">✏️</a>
                <form action="/contacts/{{this.id}}" method="POST" style="display:inline;">
                  <input type="hidden" name="_method" value="DELETE">
                  <a href="/contact-delete/{{this.id}}">🗑️</a>
                </form>
                </div>
              </li>
            {{/each}}
          </ul>
      {{else}}
        <p>Контакты отсутствуют.</p>
      {{/if}}
    <div class="do-entity-add">
    <a href="/contact-add">Добавить контакт</a>
  </div>
  </div>
</main>
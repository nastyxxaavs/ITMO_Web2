<main>
  <div class="entities">
      {{#if requests.length}}
    <h1>Запросы клиентов</h1>
    <ul>
      {{#each requests}}
        <li>
          <div class="entity-content">
          <a href="/requests/{{this.id}}"></a> - {{this.clientName}}, {{this.contactInfo}}, {{this.serviceRequested}}, {{this.requestDate}}, {{this.status}}
          <a href="/request-edit/{{this.id}}">✏️</a>
          <form action="/requests/{{this.id}}" method="POST" style="display:inline;">
            <input type="hidden" name="_method" value="DELETE">
            <a href="/request-delete/{{this.id}}">🗑️</a>
          </form>
          </div>
        </li>
      {{/each}}
    </ul>
      {{else}}
        <p>Заказы отсутствуют.</p>
      {{/if}}
    <div class="do-entity-add">
    <a href="/request-add">Создать новый запрос</a>
    </div>
  </div>
</main>